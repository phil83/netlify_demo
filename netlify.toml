[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run vite"
  targetPort = 5173
  port = 8888
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
